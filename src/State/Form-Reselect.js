export const getDataForm = state => {
  return state.formData.dataForm;
};

export const getIsFormValid = state => {
  return state.formData.isFormValid;
};

export const getIsAccount = state => {
  return state.formData.isAccount;
};